# CryptoEdge v20.8.0

- Dodano formularz API Key, API Secret i Passphrase BloFin w React Ustawienia.
- Klucze są zapisywane przez istniejący szyfrowany magazyn `secrets.bin`.
- UI otrzymuje wyłącznie stan konfiguracji i zamaskowaną końcówkę API Key.
- Dodano bezpieczne usuwanie kluczy oraz test połączenia read-only.
- Test pokazuje saldo, dostępne środki i liczbę otwartych pozycji bez składania zleceń.
