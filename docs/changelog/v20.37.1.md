# v20.37.1 — testy i baseline'y dogonily strojenie z 31.08–01.09

Utrzymanie, nie nowa funkcja. Zastalem drzewo robocze z **niezatwierdzonym
strojeniem strategii V2** (config.py, daytrading_engine_v2.py,
v2_trade_lifecycle.py + przepobrane data/replay/*_30d.json) i pakietem
w stanie **34 bledy**. Kod strojenia jest cudzy i **nie zostal ruszony** —
zmienione sa wylacznie testy i baseline'y bramek.

## Skad bralo sie 34 bledy

| grupa | ile | przyczyna |
|---|---|---|
| kaskada V2, funding, profile | 25 | nowe sitko `DAYTRADING_V2_MAX_SL_PCT` |
| stale czasowe i TP | 3 | 10h→48h, 10h→24h, TP1 2.0R→1.8R |
| koszty round-trip | 1 | slippage 0.0008→0.0003 |
| przelacznik HTF | 1 | `EXIT_ON_HTF_REVERSAL` False→True |
| baseline'y 4 bramek | 4 | zmiana hash konfiguracji |

### Sitko SL: 25 testow padalo na czyms, czego nie badaly

Atrapy w `test_daytrading_engine_v2` stoja na skali 100–120, wiec swing ma
rozpietosc 20%, a wejscie na retracement 0.5 lezy **11.2% nad SL**
(112 → 99.5). Nowe sitko `MAX_SL_PCT = 0.10` odrzucalo przez to **kazdy**
przypadek, zanim kaskada zdazyla cokolwiek powiedziec. To artefakt skali
syntetycznych liczb, nie wypowiedz o strategii.

Zamiast poprawiac 25 asercji: sitko jest wylaczane w `setUp` trzech klas
(`0.0` — tak czyta to silnik), z komentarzem dlaczego. Za to **samo sitko
dostalo pierwsze wlasne pokrycie** (`TestSlWidthSieve`, 3 przypadki):
odrzuca przy progu 0.10, przepuszcza ten sam scenariusz przy 0.20,
i respektuje 0 jako wylaczenie — na tym ostatnim opiera sie `setUp`
tamtych klas, wiec gdyby semantyka sie zmienila, 25 testow zaczeloby
po cichu badac cos innego.

### Przelacznik HTF: test polegal na domyslce

`test_htf_reversal_does_not_close_by_default` zakladal, ze domyslka to
`False`. Po zmianie przestal cokolwiek znaczyc. Teraz ustawia flage
**wprost** (`..._when_flag_off`) i razem z blizniakiem pinuje obie strony
przelacznika niezaleznie od domyslki. Doszedl osobny
`test_htf_reversal_is_on_by_default_since_01_09_2026`, zeby kolejna zmiana
domyslnej wartosci byla glosna.

## Co strojenie zmienilo w zachowaniu — wedlug bramek

| bramka | zmienionych przypadkow |
|---|---|
| `risk_gate` | **0 / 143** |
| `entry_gate` | **0 / 22** |
| `exec_gate` | **0 / 33** |
| `exit_gate` | 35 / 63 — **tylko kwoty** |

W `exit_gate` **zaden powod wyjscia sie nie zmienil** (0 zmian). Zmienil sie
wylacznie kapital: 35 przypadkow, **wszystkie na plus**, od +0.0625 do
+0.25 USD przy koncie 10 000 — dokladnie podpis nizszego poslizgu.

Diff baseline'ow to potwierdza wprost: w `risk_gate`, `entry_gate`
i `exec_gate` zmienily sie **trzy linie kazda** — hash, liczba kluczy
(531→533) i obserwowana wartosc hard stopu. Ani jeden wynik przypadku.

**Wniosek: strojenie nie ruszylo logiki decyzji, wejscia ani zlecen na
venue.** Dotknelo ksiegowania kosztow i — poza bramkami — samego doboru
setupow.

## UWAGA: parity pokazuje gorszy wynik, ale porownanie jest ZANIECZYSZCZONE

`parity.py` raportuje:

```
przed:  64 transakcji | net  -6.5366R | OOS  +2.2130R (8 transakcji)
po   :  70 transakcji | net -11.0035R | OOS  -6.1288R (25 transakcji)
```

Lejek odrzucen pokazuje mechanizm: `V2_SL_TOO_WIDE 0 → 393`,
`V2_NO_15M_TRIGGER 4716 → 3360`, `V2_SL_TOO_TIGHT_VS_COST 2075 → 1424`,
`V2_LOSS_STREAK_PAUSE 1 → 14`. W liscie transakcji widac **strumien nowych
wyjsc `htf_reversal`, niemal wylacznie ujemnych** (−0.89, −0.71, −0.63,
−0.44, −0.38, −0.22, −0.15, −0.14, −0.11 R...) — czyli dokladnie to, przed
czym ostrzegal usuniety komentarz w configu: *"90D WF: 977/1082 zamkniec =
htf_reversal"*.

**Ale tego wyniku NIE WOLNO czytac jako oceny strojenia.** Pliki
`data/replay/*_30d.json` zostaly w miedzyczasie **pobrane na nowo**
(~254 tys. linii wymienionych, inne `downloaded_at`), wiec baseline stoi na
innym oknie rynkowym niz obecny przebieg. Porownanie miesza dwie zmiany:
inne parametry **i** inne dane. Rozdzielic je moze dopiero przebieg
obecnego configu na **starych** danych.

Baseline `parity.py` zostal **swiadomie nietkniety** — przepisanie go
skasowaloby jedyny slad po tej roznicy.

## Przy okazji: 7 zduplikowanych kluczy w config.py

`STRATEGY_MODE`, `TRADITIONAL_MARKET_SYMBOLS`, `DAYTRADING_V2_15M_LOOKBACK`,
`DAYTRADING_V2_15M_RECLAIM_BARS`, `DAYTRADING_V2_BE_AFTER_TP2`,
`DAYTRADING_V2_SWING_MIN_BARS`, `DAYTRADING_V2_SWING_RIGHT_CONFIRM`.

W Pythonie wygrywa po cichu ostatnie przypisanie. Teraz wartosci sie
zgadzaja, wiec nic nie boli — ale zmiana tego "pierwszego" nie zrobi nic.
**Nie poprawione**, bo to plik z cudza praca w toku.

## Dowody

- pakiet: **1199 testow OK** (bylo 34 bledy);
- bramki: bez roznic wobec przepisanych baseline'ow;
- `parity.py`: rozni sie — patrz wyzej, celowo nie przepisany.

## Czego to wydanie NIE robi

- nie dotyka `config.py`, `daytrading_engine_v2.py`, `v2_trade_lifecycle.py`
  ani plikow replay — to cudza, niezatwierdzona praca;
- nie ocenia strojenia (dane sie zmienily razem z parametrami);
- nie przepisuje baseline'u parity;
- `port.reduce` dla recznego zamkniecia czeka — nie da sie go uczciwie
  zweryfikowac, dopoki punkt wyjscia nie jest zielony i ustalony.
