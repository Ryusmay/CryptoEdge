# CryptoEdge v19.25.19

1. V2: `Position` nie nadpisuje swing SL, gdy brak `tp_price`.
2. CLOSE ALL woła `rt.close_all()` — pozycje zamykane, silnik nie halt.
3. SET: DEMO/LIVE tylko przez Account Mode (checkbox PAPER_TRADING usunięty).
