# CryptoEdge v20.11.6

- Usunięto blokujące wywołanie `show()` wykonywane przed startem pętli zdarzeń Tauri.
- Okno jest tworzone przez konfigurację Tauri i uruchamiane bez flagi `SW_HIDE`.
- Zweryfikowano właściwą nazwę procesu nowego buildu (`cryptoedge-terminal`).
